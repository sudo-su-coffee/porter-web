{
  "info": {
    "name": "Porter API",
    "description": "Generated by scripts/gen_postman.py from apiRoutes in backend/internal/api/api.go (321 routes). Run folder '00 bootstrap' first, then any group. Set member_token for the RBAC negative folder. Requests tagged [destructive] mutate real state; skip them in blind runs.",
    "schema": "https://schema.getpostman.com/json/collection/v2.1.0/collection.json"
  },
  "variable": [
    {
      "key": "base_url",
      "value": "http://127.0.0.1:8080"
    },
    {
      "key": "admin_user",
      "value": "admin"
    },
    {
      "key": "admin_pass",
      "value": ""
    },
    {
      "key": "token",
      "value": ""
    },
    {
      "key": "csrf",
      "value": ""
    },
    {
      "key": "member_token",
      "value": ""
    },
    {
      "key": "groupId",
      "value": ""
    },
    {
      "key": "projectId",
      "value": ""
    },
    {
      "key": "roleId",
      "value": ""
    },
    {
      "key": "username",
      "value": ""
    },
    {
      "key": "alertId",
      "value": ""
    },
    {
      "key": "buildId",
      "value": ""
    },
    {
      "key": "checkName",
      "value": ""
    },
    {
      "key": "cronId",
      "value": ""
    },
    {
      "key": "deployId",
      "value": ""
    },
    {
      "key": "domainId",
      "value": ""
    },
    {
      "key": "drainId",
      "value": ""
    },
    {
      "key": "envId",
      "value": ""
    },
    {
      "key": "hookId",
      "value": ""
    },
    {
      "key": "id",
      "value": ""
    },
    {
      "key": "keyId",
      "value": ""
    },
    {
      "key": "n",
      "value": ""
    },
    {
      "key": "permissionId",
      "value": ""
    },
    {
      "key": "redirectId",
      "value": ""
    },
    {
      "key": "reference",
      "value": ""
    },
    {
      "key": "replicaId",
      "value": ""
    },
    {
      "key": "ruleId",
      "value": ""
    },
    {
      "key": "secretId",
      "value": ""
    },
    {
      "key": "serviceName",
      "value": ""
    },
    {
      "key": "volumeId",
      "value": ""
    }
  ],
  "item": [
    {
      "name": "00 bootstrap",
      "item": [
        {
          "name": "POST /auth/login",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "pm.test('public route reachable', () => pm.expect(pm.response.code).to.not.equal(401));",
                  "if (pm.response.code === 200) { pm.collectionVariables.set('token', pm.response.json().token); }"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/auth/login",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "login"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: public",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"username\": \"{{admin_user}}\",\n  \"password\": \"{{admin_pass}}\"\n}"
            }
          }
        },
        {
          "name": "GET /csrf",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "if (pm.response.code === 200) { pm.collectionVariables.set('csrf', pm.response.json().csrf_token); }"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/csrf",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "csrf"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "01 rbac negative",
      "item": [
        {
          "name": "no token -> 401 on /projects",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('expect [401]', () => pm.expect(pm.response.code).to.be.oneOf([401]));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/projects",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects"
              ]
            }
          }
        },
        {
          "name": "no token -> 401 on /users",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('expect [401]', () => pm.expect(pm.response.code).to.be.oneOf([401]));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/users",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "users"
              ]
            }
          }
        },
        {
          "name": "bad token -> 401",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('expect [401]', () => pm.expect(pm.response.code).to.be.oneOf([401]));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/projects",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects"
              ]
            }
          }
        },
        {
          "name": "write without CSRF -> 403",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('expect [403]', () => pm.expect(pm.response.code).to.be.oneOf([403]));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "member cannot list users -> 403",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('expect [403]', () => pm.expect(pm.response.code).to.be.oneOf([403]));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{member_token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/users",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "users"
              ]
            }
          }
        },
        {
          "name": "member cannot create role -> 403",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('expect [403]', () => pm.expect(pm.response.code).to.be.oneOf([403]));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{member_token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/roles",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "roles"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "member cannot delete user -> 403",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('expect [403]', () => pm.expect(pm.response.code).to.be.oneOf([403]));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{member_token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/users/nobody",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "users",
                "nobody"
              ]
            }
          }
        },
        {
          "name": "member cannot register server -> 403",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('expect [403]', () => pm.expect(pm.response.code).to.be.oneOf([403]));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{member_token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/servers",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "servers"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "member cannot prune images -> 403",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('expect [403]', () => pm.expect(pm.response.code).to.be.oneOf([403]));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{member_token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/images/prune",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "images",
                "prune"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "wrong login -> 401",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "pm.test('expect [401]', () => pm.expect(pm.response.code).to.be.oneOf([401]));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/auth/login",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "login"
              ]
            },
            "body": {
              "mode": "raw",
              "raw": "{\"username\": \"nobody\", \"password\": \"wrong\"}"
            }
          }
        }
      ]
    },
    {
      "name": "10 auth",
      "item": [
        {
          "name": "POST /auth/login",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "pm.test('public route reachable', () => pm.expect(pm.response.code).to.not.equal(401));",
                  "if (pm.response.code === 200) { pm.collectionVariables.set('token', pm.response.json().token); }"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/auth/login",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "login"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: public",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"username\": \"{{admin_user}}\",\n  \"password\": \"{{admin_pass}}\"\n}"
            }
          }
        },
        {
          "name": "POST /auth/logout",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "pm.test('public route reachable', () => pm.expect(pm.response.code).to.not.equal(401));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/auth/logout",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "logout"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: public",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /auth/signup",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "pm.test('public route reachable', () => pm.expect(pm.response.code).to.not.equal(401));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/auth/signup",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "signup"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: public",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /auth/password/forgot",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "pm.test('public route reachable', () => pm.expect(pm.response.code).to.not.equal(401));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/auth/password/forgot",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "password",
                "forgot"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: public",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /auth/password/reset",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "pm.test('public route reachable', () => pm.expect(pm.response.code).to.not.equal(401));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/auth/password/reset",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "password",
                "reset"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: public",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /auth/jwks",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "pm.test('public route reachable', () => pm.expect(pm.response.code).to.not.equal(401));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/auth/jwks",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "jwks"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: public"
          }
        },
        {
          "name": "POST /auth/token",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/auth/token",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "token"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /auth/session",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/auth/session",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "auth",
                "session"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 csrf",
      "item": [
        {
          "name": "GET /csrf",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "if (pm.response.code === 200) { pm.collectionVariables.set('csrf', pm.response.json().csrf_token); }"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/csrf",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "csrf"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 feedback",
      "item": [
        {
          "name": "POST /feedback",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/feedback",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "feedback"
              ],
              "variable": []
            },
            "description": "perm: `feedback.write` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /feedback",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/feedback",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "feedback"
              ],
              "variable": []
            },
            "description": "perm: `feedback.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 global",
      "item": [
        {
          "name": "GET /global/analytics",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/global/analytics",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "global",
                "analytics"
              ],
              "variable": []
            },
            "description": "perm: `analytics.read` | auth: bearer"
          }
        },
        {
          "name": "GET /global/analytics/timeseries",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/global/analytics/timeseries",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "global",
                "analytics",
                "timeseries"
              ],
              "variable": []
            },
            "description": "perm: `analytics.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 groups",
      "item": [
        {
          "name": "GET /groups",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/groups",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "groups"
              ],
              "variable": []
            },
            "description": "perm: `group.create` | auth: bearer"
          }
        },
        {
          "name": "POST /groups",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/groups",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "groups"
              ],
              "variable": []
            },
            "description": "perm: `group.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /groups/{groupId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/groups/:groupId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "groups",
                ":groupId"
              ],
              "variable": [
                {
                  "key": "groupId",
                  "value": "{{groupId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PATCH /groups/{groupId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/groups/:groupId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "groups",
                ":groupId"
              ],
              "variable": [
                {
                  "key": "groupId",
                  "value": "{{groupId}}"
                }
              ]
            },
            "description": "perm: `group.update` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /groups/{groupId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/groups/:groupId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "groups",
                ":groupId"
              ],
              "variable": [
                {
                  "key": "groupId",
                  "value": "{{groupId}}"
                }
              ]
            },
            "description": "perm: `group.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /groups/{groupId}/projects",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/groups/:groupId/projects",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "groups",
                ":groupId",
                "projects"
              ],
              "variable": [
                {
                  "key": "groupId",
                  "value": "{{groupId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "POST /groups/{groupId}/projects/{projectId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/groups/:groupId/projects/:projectId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "groups",
                ":groupId",
                "projects",
                ":projectId"
              ],
              "variable": [
                {
                  "key": "groupId",
                  "value": "{{groupId}}"
                },
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.write` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /groups/{groupId}/projects/{projectId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/groups/:groupId/projects/:projectId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "groups",
                ":groupId",
                "projects",
                ":projectId"
              ],
              "variable": [
                {
                  "key": "groupId",
                  "value": "{{groupId}}"
                },
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.write` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 guest-bases",
      "item": [
        {
          "name": "GET /guest-bases",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/guest-bases",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "guest-bases"
              ],
              "variable": []
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 health",
      "item": [
        {
          "name": "GET /health",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "pm.test('public route reachable', () => pm.expect(pm.response.code).to.not.equal(401));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/health",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "health"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: public"
          }
        }
      ]
    },
    {
      "name": "10 healthz",
      "item": [
        {
          "name": "GET /healthz",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "pm.test('public route reachable', () => pm.expect(pm.response.code).to.not.equal(401));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/healthz",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "healthz"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: public"
          }
        }
      ]
    },
    {
      "name": "10 host",
      "item": [
        {
          "name": "GET /host/overview",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/host/overview",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "host",
                "overview"
              ],
              "variable": []
            },
            "description": "perm: `metric.read` | auth: bearer"
          }
        },
        {
          "name": "GET /host/ports",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/host/ports",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "host",
                "ports"
              ],
              "variable": []
            },
            "description": "perm: `metric.read` | auth: bearer"
          }
        },
        {
          "name": "GET /host/kernel",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/host/kernel",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "host",
                "kernel"
              ],
              "variable": []
            },
            "description": "perm: `metric.read` | auth: bearer"
          }
        },
        {
          "name": "GET /host/prerequisites",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/host/prerequisites",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "host",
                "prerequisites"
              ],
              "variable": []
            },
            "description": "perm: `metric.read` | auth: bearer"
          }
        },
        {
          "name": "GET /host/runtime",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/host/runtime",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "host",
                "runtime"
              ],
              "variable": []
            },
            "description": "perm: `metric.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 images",
      "item": [
        {
          "name": "GET /images",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/images",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "images"
              ],
              "variable": []
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "GET /images/base",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/images/base",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "images",
                "base"
              ],
              "variable": []
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "GET /images/base/readiness",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/images/base/readiness",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "images",
                "base",
                "readiness"
              ],
              "variable": []
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "POST /images/custom",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/images/custom",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "images",
                "custom"
              ],
              "variable": []
            },
            "description": "perm: `image.upload` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /images/search",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/images/search",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "images",
                "search"
              ],
              "variable": []
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "GET /images/{reference}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/images/:reference",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "images",
                ":reference"
              ],
              "variable": [
                {
                  "key": "reference",
                  "value": "{{reference}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "[destructive] DELETE /images/{reference}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/images/:reference",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "images",
                ":reference"
              ],
              "variable": [
                {
                  "key": "reference",
                  "value": "{{reference}}"
                }
              ]
            },
            "description": "perm: `project.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] POST /images/prune",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/images/prune",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "images",
                "prune"
              ],
              "variable": []
            },
            "description": "perm: `cache.purge` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /images/stats",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/images/stats",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "images",
                "stats"
              ],
              "variable": []
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "GET /images/ml",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/images/ml",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "images",
                "ml"
              ],
              "variable": []
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 login",
      "item": [
        {
          "name": "POST /login",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "pm.test('public route reachable', () => pm.expect(pm.response.code).to.not.equal(401));",
                  "if (pm.response.code === 200) { pm.collectionVariables.set('token', pm.response.json().token); }"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/login",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "login"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: public",
            "body": {
              "mode": "raw",
              "raw": "{\n  \"username\": \"{{admin_user}}\",\n  \"password\": \"{{admin_pass}}\"\n}"
            }
          }
        }
      ]
    },
    {
      "name": "10 logout",
      "item": [
        {
          "name": "POST /logout",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "pm.test('public route reachable', () => pm.expect(pm.response.code).to.not.equal(401));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/logout",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "logout"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: public",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 logs",
      "item": [
        {
          "name": "GET /logs",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/logs",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "logs"
              ],
              "variable": []
            },
            "description": "perm: `log.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 org",
      "item": [
        {
          "name": "GET /org",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/org",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "org"
              ],
              "variable": []
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PATCH /org",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/org",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "org"
              ],
              "variable": []
            },
            "description": "perm: `org.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 orgs",
      "item": [
        {
          "name": "GET /orgs",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/orgs",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "orgs"
              ],
              "variable": []
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "GET /orgs/default",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/orgs/default",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "orgs",
                "default"
              ],
              "variable": []
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "POST /orgs",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/orgs",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "orgs"
              ],
              "variable": []
            },
            "description": "perm: `org.member.role` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /orgs/current",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/orgs/current",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "orgs",
                "current"
              ],
              "variable": []
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PATCH /orgs/current",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/orgs/current",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "orgs",
                "current"
              ],
              "variable": []
            },
            "description": "perm: `org.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /orgs/current",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/orgs/current",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "orgs",
                "current"
              ],
              "variable": []
            },
            "description": "perm: `org.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /orgs/members",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/orgs/members",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "orgs",
                "members"
              ],
              "variable": []
            },
            "description": "perm: `member.list` | auth: bearer"
          }
        },
        {
          "name": "POST /orgs/members",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/orgs/members",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "orgs",
                "members"
              ],
              "variable": []
            },
            "description": "perm: `org.member.add` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "PATCH /orgs/members/{username}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/orgs/members/:username",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "orgs",
                "members",
                ":username"
              ],
              "variable": [
                {
                  "key": "username",
                  "value": "{{username}}"
                }
              ]
            },
            "description": "perm: `org.member.role` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /orgs/members/{username}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/orgs/members/:username",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "orgs",
                "members",
                ":username"
              ],
              "variable": [
                {
                  "key": "username",
                  "value": "{{username}}"
                }
              ]
            },
            "description": "perm: `org.member.remove` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /orgs/audit",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/orgs/audit",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "orgs",
                "audit"
              ],
              "variable": []
            },
            "description": "perm: `org.audit` | auth: bearer"
          }
        },
        {
          "name": "[destructive] POST /orgs/transfer",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/orgs/transfer",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "orgs",
                "transfer"
              ],
              "variable": []
            },
            "description": "perm: `org.transfer` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /orgs/events",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/orgs/events",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "orgs",
                "events"
              ],
              "variable": []
            },
            "description": "perm: `event.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 overview",
      "item": [
        {
          "name": "GET /overview",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/overview",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "overview"
              ],
              "variable": []
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 permissions",
      "item": [
        {
          "name": "GET /permissions",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/permissions",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "permissions"
              ],
              "variable": []
            },
            "description": "perm: `org.audit` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects",
      "item": [
        {
          "name": "GET /projects",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects"
              ],
              "variable": []
            },
            "description": "perm: `project.list` | auth: bearer"
          }
        },
        {
          "name": "POST /projects",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects"
              ],
              "variable": []
            },
            "description": "perm: `project.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/compose",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/compose",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                "compose"
              ],
              "variable": []
            },
            "description": "perm: `project.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PATCH /projects/{projectId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.rename` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/alerts",
      "item": [
        {
          "name": "GET /projects/{projectId}/alerts",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/alerts",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "alerts"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/alerts",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/alerts",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "alerts"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `alert.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/alerts/{alertId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/alerts/:alertId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "alerts",
                ":alertId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "alertId",
                  "value": "{{alertId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PATCH /projects/{projectId}/alerts/{alertId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/alerts/:alertId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "alerts",
                ":alertId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "alertId",
                  "value": "{{alertId}}"
                }
              ]
            },
            "description": "perm: `alert.update` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/alerts/{alertId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/alerts/:alertId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "alerts",
                ":alertId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "alertId",
                  "value": "{{alertId}}"
                }
              ]
            },
            "description": "perm: `alert.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/alerts/{alertId}/silence",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/alerts/:alertId/silence",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "alerts",
                ":alertId",
                "silence"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "alertId",
                  "value": "{{alertId}}"
                }
              ]
            },
            "description": "perm: `alert.silence` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/alerts/{alertId}/unsilence",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/alerts/:alertId/unsilence",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "alerts",
                ":alertId",
                "unsilence"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "alertId",
                  "value": "{{alertId}}"
                }
              ]
            },
            "description": "perm: `alert.silence` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/analytics",
      "item": [
        {
          "name": "GET /projects/{projectId}/analytics/usage",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/analytics/usage",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "analytics",
                "usage"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `analytics.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/analytics/usage/timeseries",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/analytics/usage/timeseries",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "analytics",
                "usage",
                "timeseries"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `analytics.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/analytics/paths",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/analytics/paths",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "analytics",
                "paths"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `analytics.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/analytics/status-codes",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/analytics/status-codes",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "analytics",
                "status-codes"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `analytics.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/analytics/bandwidth",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/analytics/bandwidth",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "analytics",
                "bandwidth"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `analytics.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/analytics/requests",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/analytics/requests",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "analytics",
                "requests"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `analytics.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/analytics/invocations",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/analytics/invocations",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "analytics",
                "invocations"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `analytics.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/autoscale",
      "item": [
        {
          "name": "GET /projects/{projectId}/autoscale",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/autoscale",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "autoscale"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/autoscale",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/autoscale",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "autoscale"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/avatar",
      "item": [
        {
          "name": "POST /projects/{projectId}/avatar",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/avatar",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "avatar"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.avatar` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/builds",
      "item": [
        {
          "name": "GET /projects/{projectId}/builds",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/builds",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "builds"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `build.create` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/builds",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/builds",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "builds"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `build.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/builds/run",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/builds/run",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "builds",
                "run"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `build.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/builds/{buildId}/logs",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/builds/:buildId/logs",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "builds",
                ":buildId",
                "logs"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "buildId",
                  "value": "{{buildId}}"
                }
              ]
            },
            "description": "perm: `log.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/builds/{buildId}/logs/stream",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/builds/:buildId/logs/stream",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "builds",
                ":buildId",
                "logs",
                "stream"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "buildId",
                  "value": "{{buildId}}"
                }
              ]
            },
            "description": "perm: `log.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/cache",
      "item": [
        {
          "name": "GET /projects/{projectId}/cache/stats",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/cache/stats",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "cache",
                "stats"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `cache.stats` | auth: bearer"
          }
        },
        {
          "name": "[destructive] POST /projects/{projectId}/cache/purge",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/cache/purge",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "cache",
                "purge"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `cache.purge` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] POST /projects/{projectId}/cache/purge/path",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/cache/purge/path",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "cache",
                "purge",
                "path"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `cache.purge` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/compose",
      "item": [
        {
          "name": "GET /projects/{projectId}/compose",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/compose",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "compose"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/compose",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/compose",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "compose"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.import` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/compose/validate",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/compose/validate",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "compose",
                "validate"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.import` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/compose/preview",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/compose/preview",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "compose",
                "preview"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/crons",
      "item": [
        {
          "name": "GET /projects/{projectId}/crons",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/crons",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "crons"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `cron.create` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/crons",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/crons",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "crons"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `cron.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/crons/history",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/crons/history",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "crons",
                "history"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `cron.update` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/crons/{cronId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/crons/:cronId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "crons",
                ":cronId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "cronId",
                  "value": "{{cronId}}"
                }
              ]
            },
            "description": "perm: `cron.create` | auth: bearer"
          }
        },
        {
          "name": "PATCH /projects/{projectId}/crons/{cronId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/crons/:cronId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "crons",
                ":cronId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "cronId",
                  "value": "{{cronId}}"
                }
              ]
            },
            "description": "perm: `cron.update` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/crons/{cronId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/crons/:cronId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "crons",
                ":cronId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "cronId",
                  "value": "{{cronId}}"
                }
              ]
            },
            "description": "perm: `cron.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/crons/{cronId}/run",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/crons/:cronId/run",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "crons",
                ":cronId",
                "run"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "cronId",
                  "value": "{{cronId}}"
                }
              ]
            },
            "description": "perm: `cron.run` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/deployments",
      "item": [
        {
          "name": "GET /projects/{projectId}/deployments",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `deployment.list` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/deployments",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `deployment.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/deployments/upload",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments/upload",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments",
                "upload"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `deployment.create` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/deployments/{deployId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments/:deployId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments",
                ":deployId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "deployId",
                  "value": "{{deployId}}"
                }
              ]
            },
            "description": "perm: `deployment.list` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/deployments/{deployId}/checks",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments/:deployId/checks",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments",
                ":deployId",
                "checks"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "deployId",
                  "value": "{{deployId}}"
                }
              ]
            },
            "description": "perm: `deployment.list` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/deployments/{deployId}/checks",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments/:deployId/checks",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments",
                ":deployId",
                "checks"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "deployId",
                  "value": "{{deployId}}"
                }
              ]
            },
            "description": "perm: `deployment.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "PATCH /projects/{projectId}/deployments/{deployId}/checks/{checkName}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments/:deployId/checks/:checkName",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments",
                ":deployId",
                "checks",
                ":checkName"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "deployId",
                  "value": "{{deployId}}"
                },
                {
                  "key": "checkName",
                  "value": "{{checkName}}"
                }
              ]
            },
            "description": "perm: `deployment.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "PUT /projects/{projectId}/deployments/{deployId}/rollout",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments/:deployId/rollout",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments",
                ":deployId",
                "rollout"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "deployId",
                  "value": "{{deployId}}"
                }
              ]
            },
            "description": "perm: `deployment.promote` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/deployments/{deployId}/logs",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments/:deployId/logs",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments",
                ":deployId",
                "logs"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "deployId",
                  "value": "{{deployId}}"
                }
              ]
            },
            "description": "perm: `log.read` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/deployments/{deployId}/promote",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments/:deployId/promote",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments",
                ":deployId",
                "promote"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "deployId",
                  "value": "{{deployId}}"
                }
              ]
            },
            "description": "perm: `deployment.promote` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/deployments/{deployId}/rollback",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments/:deployId/rollback",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments",
                ":deployId",
                "rollback"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "deployId",
                  "value": "{{deployId}}"
                }
              ]
            },
            "description": "perm: `deployment.rollback` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/deployments/{deployId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments/:deployId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments",
                ":deployId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "deployId",
                  "value": "{{deployId}}"
                }
              ]
            },
            "description": "perm: `deployment.rollback` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/deployments/{deployId}/source",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments/:deployId/source",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments",
                ":deployId",
                "source"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "deployId",
                  "value": "{{deployId}}"
                }
              ]
            },
            "description": "perm: `deployment.list` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/deployments/{deployId}/og",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments/:deployId/og",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments",
                ":deployId",
                "og"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "deployId",
                  "value": "{{deployId}}"
                }
              ]
            },
            "description": "perm: `deployment.list` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/deployments/git",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/deployments/git",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "deployments",
                "git"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `build.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/dns",
      "item": [
        {
          "name": "GET /projects/{projectId}/dns",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/dns",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "dns"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `domain.list` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/dns/records",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/dns/records",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "dns",
                "records"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `domain.list` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/domains",
      "item": [
        {
          "name": "GET /projects/{projectId}/domains",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/domains",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "domains"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `domain.list` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/domains",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/domains",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "domains"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `domain.add` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/domains/records",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/domains/records",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "domains",
                "records"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `domain.list` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/domains/{domainId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/domains/:domainId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "domains",
                ":domainId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "domainId",
                  "value": "{{domainId}}"
                }
              ]
            },
            "description": "perm: `domain.list` | auth: bearer"
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/domains/{domainId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/domains/:domainId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "domains",
                ":domainId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "domainId",
                  "value": "{{domainId}}"
                }
              ]
            },
            "description": "perm: `domain.remove` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/domains/{domainId}/verify",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/domains/:domainId/verify",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "domains",
                ":domainId",
                "verify"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "domainId",
                  "value": "{{domainId}}"
                }
              ]
            },
            "description": "perm: `domain.verify` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/domains/{domainId}/reverify",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/domains/:domainId/reverify",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "domains",
                ":domainId",
                "reverify"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "domainId",
                  "value": "{{domainId}}"
                }
              ]
            },
            "description": "perm: `domain.verify` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/drains",
      "item": [
        {
          "name": "GET /projects/{projectId}/drains",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/drains",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "drains"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/drains",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/drains",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "drains"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `drain.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/drains/{drainId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/drains/:drainId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "drains",
                ":drainId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "drainId",
                  "value": "{{drainId}}"
                }
              ]
            },
            "description": "perm: `drain.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/drains/{drainId}/test",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/drains/:drainId/test",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "drains",
                ":drainId",
                "test"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "drainId",
                  "value": "{{drainId}}"
                }
              ]
            },
            "description": "perm: `drain.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/env",
      "item": [
        {
          "name": "GET /projects/{projectId}/env",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/env",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "env"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `env.list` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/env",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/env",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "env"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `env.set` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/env/bulk",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/env/bulk",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "env",
                "bulk"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `env.set` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "PATCH /projects/{projectId}/env/{envId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/env/:envId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "env",
                ":envId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "envId",
                  "value": "{{envId}}"
                }
              ]
            },
            "description": "perm: `env.set` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/env/{envId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/env/:envId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "env",
                ":envId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "envId",
                  "value": "{{envId}}"
                }
              ]
            },
            "description": "perm: `env.set` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/environments",
      "item": [
        {
          "name": "GET /projects/{projectId}/environments",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/environments",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "environments"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/environments",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/environments",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "environments"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/environments/available",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/environments/available",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "environments",
                "available"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/environments/{envId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/environments/:envId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "environments",
                ":envId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "envId",
                  "value": "{{envId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PATCH /projects/{projectId}/environments/{envId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/environments/:envId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "environments",
                ":envId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "envId",
                  "value": "{{envId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/environments/{envId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/environments/:envId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "environments",
                ":envId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "envId",
                  "value": "{{envId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/environments/{envId}/branch",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/environments/:envId/branch",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "environments",
                ":envId",
                "branch"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "envId",
                  "value": "{{envId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/environments/{envId}/domain",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/environments/:envId/domain",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "environments",
                ":envId",
                "domain"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "envId",
                  "value": "{{envId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/environments/{envId}/range",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/environments/:envId/range",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "environments",
                ":envId",
                "range"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "envId",
                  "value": "{{envId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/events",
      "item": [
        {
          "name": "GET /projects/{projectId}/events",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/events",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "events"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `event.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/export",
      "item": [
        {
          "name": "POST /projects/{projectId}/export",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/export",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "export"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.export` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/firewall",
      "item": [
        {
          "name": "GET /projects/{projectId}/firewall/rules",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/firewall/rules",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "firewall",
                "rules"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/firewall/rules",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/firewall/rules",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "firewall",
                "rules"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `firewall.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/firewall/rules/{ruleId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/firewall/rules/:ruleId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "firewall",
                "rules",
                ":ruleId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "ruleId",
                  "value": "{{ruleId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/firewall/rules/{ruleId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/firewall/rules/:ruleId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "firewall",
                "rules",
                ":ruleId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "ruleId",
                  "value": "{{ruleId}}"
                }
              ]
            },
            "description": "perm: `firewall.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "PATCH /projects/{projectId}/firewall/rules/{ruleId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/firewall/rules/:ruleId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "firewall",
                "rules",
                ":ruleId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "ruleId",
                  "value": "{{ruleId}}"
                }
              ]
            },
            "description": "perm: `firewall.update` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/firewall/events",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/firewall/events",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "firewall",
                "events"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `traffic.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/firewall/stats",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/firewall/stats",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "firewall",
                "stats"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `traffic.read` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/firewall/whitelist",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/firewall/whitelist",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "firewall",
                "whitelist"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `firewall.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/git",
      "item": [
        {
          "name": "POST /projects/{projectId}/git/import",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/git/import",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "git",
                "import"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `git.import` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/git/branches",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/git/branches",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "git",
                "branches"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `git.import` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/healthcheck",
      "item": [
        {
          "name": "GET /projects/{projectId}/healthcheck",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/healthcheck",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "healthcheck"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/healthcheck",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/healthcheck",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "healthcheck"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/hooks",
      "item": [
        {
          "name": "GET /projects/{projectId}/hooks",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/hooks",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "hooks"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/hooks",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/hooks",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "hooks"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `hook.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/hooks/{hookId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/hooks/:hookId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "hooks",
                ":hookId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "hookId",
                  "value": "{{hookId}}"
                }
              ]
            },
            "description": "perm: `hook.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/hooks/{hookId}/trigger",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/hooks/:hookId/trigger",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "hooks",
                ":hookId",
                "trigger"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "hookId",
                  "value": "{{hookId}}"
                }
              ]
            },
            "description": "perm: `hook.trigger` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/import",
      "item": [
        {
          "name": "POST /projects/{projectId}/import",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/import",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "import"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.import` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/liveness",
      "item": [
        {
          "name": "GET /projects/{projectId}/liveness",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/liveness",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "liveness"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/logs",
      "item": [
        {
          "name": "GET /projects/{projectId}/logs",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/logs",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "logs"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `log.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/logs/stream",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/logs/stream",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "logs",
                "stream"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `log.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/members",
      "item": [
        {
          "name": "GET /projects/{projectId}/members",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/members",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "members"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `member.list` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/members",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/members",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "members"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `member.invite` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/members/{username}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/members/:username",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "members",
                ":username"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "username",
                  "value": "{{username}}"
                }
              ]
            },
            "description": "perm: `member.list` | auth: bearer"
          }
        },
        {
          "name": "PATCH /projects/{projectId}/members/{username}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/members/:username",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "members",
                ":username"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "username",
                  "value": "{{username}}"
                }
              ]
            },
            "description": "perm: `member.role` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/members/{username}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/members/:username",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "members",
                ":username"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "username",
                  "value": "{{username}}"
                }
              ]
            },
            "description": "perm: `member.remove` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/members/invite",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/members/invite",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "members",
                "invite"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `member.invite` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/metrics",
      "item": [
        {
          "name": "GET /projects/{projectId}/metrics",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/metrics",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "metrics"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `metric.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/networks",
      "item": [
        {
          "name": "GET /projects/{projectId}/networks",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/networks",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "networks"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.network` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/networks",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/networks",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "networks"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.network` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/observability",
      "item": [
        {
          "name": "GET /projects/{projectId}/observability/web-vitals",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/observability/web-vitals",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "observability",
                "web-vitals"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `webvital.read` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/observability/web-vitals/beacon",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/observability/web-vitals/beacon",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "observability",
                "web-vitals",
                "beacon"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `webvital.read` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/observability/web-vitals/timeseries",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/observability/web-vitals/timeseries",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "observability",
                "web-vitals",
                "timeseries"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `webvital.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/observability/lcp",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/observability/lcp",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "observability",
                "lcp"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `webvital.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/observability/cls",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/observability/cls",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "observability",
                "cls"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `webvital.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/observability/fid",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/observability/fid",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "observability",
                "fid"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `webvital.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/pool",
      "item": [
        {
          "name": "GET /projects/{projectId}/pool",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/pool",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "pool"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `replica.list` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/pool/drain",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/pool/drain",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "pool",
                "drain"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/redeploy",
      "item": [
        {
          "name": "POST /projects/{projectId}/redeploy",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/redeploy",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "redeploy"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.deploy` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/redirects",
      "item": [
        {
          "name": "GET /projects/{projectId}/redirects",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/redirects",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "redirects"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/redirects",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/redirects",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "redirects"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `redirect.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/redirects/{redirectId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/redirects/:redirectId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "redirects",
                ":redirectId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "redirectId",
                  "value": "{{redirectId}}"
                }
              ]
            },
            "description": "perm: `redirect.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "PUT /projects/{projectId}/redirects/bulk",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/redirects/bulk",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "redirects",
                "bulk"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `redirect.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/replicas",
      "item": [
        {
          "name": "GET /projects/{projectId}/replicas",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `replica.list` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/replicas/batch/start",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/batch/start",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                "batch",
                "start"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `replica.start` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/replicas/batch/stop",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/batch/stop",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                "batch",
                "stop"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `replica.stop` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/replicas/{n}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `replica.list` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/replicas/{n}/start",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/start",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "start"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `replica.start` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/replicas/{n}/stop",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/stop",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "stop"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `replica.stop` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/replicas/{n}/restart",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/restart",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "restart"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `replica.restart` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/replicas/{n}/snapshot",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/snapshot",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "snapshot"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `replica.snapshot` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] POST /projects/{projectId}/replicas/{n}/restore",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/restore",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "restore"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `replica.restore` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] POST /projects/{projectId}/replicas/{n}/recover",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/recover",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "recover"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `replica.restore` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/replicas/{n}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `replica.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/replicas/{n}/logs",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/logs",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "logs"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `log.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/replicas/{n}/metrics",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/metrics",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "metrics"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `metric.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/replicas/{n}/traffic",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/traffic",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "traffic"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `traffic.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/replicas/{n}/health",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/health",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "health"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `replica.list` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/replicas/{n}/ssh-info",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/ssh-info",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "ssh-info"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `ssh.connect` | auth: bearer"
          }
        },
        {
          "name": "[destructive] POST /projects/{projectId}/replicas/{n}/ssh-cert",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/ssh-cert",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "ssh-cert"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `ssh.connect` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] POST /projects/{projectId}/replicas/{n}/exec",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/exec",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "exec"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `replica.exec` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/replicas/{n}/console",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/replicas/:n/console",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "replicas",
                ":n",
                "console"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "n",
                  "value": "{{n}}"
                }
              ]
            },
            "description": "perm: `console.open` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/restart",
      "item": [
        {
          "name": "POST /projects/{projectId}/restart",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/restart",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "restart"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.restart` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/rollouts",
      "item": [
        {
          "name": "GET /projects/{projectId}/rollouts",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/rollouts",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "rollouts"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `deployment.list` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/scale",
      "item": [
        {
          "name": "GET /projects/{projectId}/scale",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/scale",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "scale"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `replica.list` | auth: bearer"
          }
        },
        {
          "name": "PATCH /projects/{projectId}/scale",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/scale",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "scale"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.scale` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/secrets",
      "item": [
        {
          "name": "GET /projects/{projectId}/secrets",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/secrets",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "secrets"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `secret.list` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/secrets",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/secrets",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "secrets"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `secret.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/secrets/{secretId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/secrets/:secretId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "secrets",
                ":secretId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "secretId",
                  "value": "{{secretId}}"
                }
              ]
            },
            "description": "perm: `secret.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/services",
      "item": [
        {
          "name": "GET /projects/{projectId}/services",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/services",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "services"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/services/{serviceName}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/services/:serviceName",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "services",
                ":serviceName"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "serviceName",
                  "value": "{{serviceName}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/services/{serviceName}/scale",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/services/:serviceName/scale",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "services",
                ":serviceName",
                "scale"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "serviceName",
                  "value": "{{serviceName}}"
                }
              ]
            },
            "description": "perm: `project.scale` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/settings",
      "item": [
        {
          "name": "GET /projects/{projectId}/settings/general",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/general",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "general"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PATCH /projects/{projectId}/settings/general",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/general",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "general"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/build",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/build",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "build"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/build",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/build",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "build"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/checks",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/checks",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "checks"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/settings/checks",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/checks",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "checks"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/rollout",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/rollout",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "rollout"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/rollout",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/rollout",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "rollout"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/build-machine",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/build-machine",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "build-machine"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/build-machine",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/build-machine",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "build-machine"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/settings/ignore-command",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/ignore-command",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "ignore-command"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `git.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/framework",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/framework",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "framework"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/git",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/git",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "git"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/git",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/git",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "git"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `git.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/settings/git/sync",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/git/sync",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "git",
                "sync"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `git.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "PATCH /projects/{projectId}/settings/git/toggles",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/git/toggles",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "git",
                "toggles"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `git.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/git/lfs",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/git/lfs",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "git",
                "lfs"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/git/lfs",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/git/lfs",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "git",
                "lfs"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `git.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/deployment-protection",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/deployment-protection",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "deployment-protection"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/deployment-protection",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/deployment-protection",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "deployment-protection"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/oidc",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/oidc",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "oidc"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/oidc",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/oidc",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "oidc"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/functions",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/functions",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "functions"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `git.settings` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/functions",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/functions",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "functions"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `git.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/security",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/security",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "security"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/security",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/security",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "security"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/retention",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/retention",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "retention"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/retention",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/retention",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "retention"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/networking",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/networking",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "networking"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.network` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/networking",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/networking",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "networking"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.network` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/advanced",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/advanced",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "advanced"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/advanced",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/advanced",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "advanced"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/passport",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/passport",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "passport"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/passport",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/passport",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "passport"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/settings/microfrontends",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/microfrontends",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "microfrontends"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        },
        {
          "name": "PUT /projects/{projectId}/settings/microfrontends",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/settings/microfrontends",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "settings",
                "microfrontends"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.settings` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/ssh",
      "item": [
        {
          "name": "PUT /projects/{projectId}/ssh",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/ssh",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "ssh"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `ssh.toggle` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/status",
      "item": [
        {
          "name": "GET /projects/{projectId}/status",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/status",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "status"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/traffic",
      "item": [
        {
          "name": "GET /projects/{projectId}/traffic",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/traffic",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "traffic"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `traffic.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 projects/transfer",
      "item": [
        {
          "name": "[destructive] POST /projects/{projectId}/transfer",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/transfer",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "transfer"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `project.transfer` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 projects/volumes",
      "item": [
        {
          "name": "GET /projects/{projectId}/volumes",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/volumes",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "volumes"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `volume.read` | auth: bearer"
          }
        },
        {
          "name": "POST /projects/{projectId}/volumes",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/volumes",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "volumes"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                }
              ]
            },
            "description": "perm: `volume.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/volumes/{volumeId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/volumes/:volumeId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "volumes",
                ":volumeId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "volumeId",
                  "value": "{{volumeId}}"
                }
              ]
            },
            "description": "perm: `volume.read` | auth: bearer"
          }
        },
        {
          "name": "[destructive] DELETE /projects/{projectId}/volumes/{volumeId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/volumes/:volumeId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "volumes",
                ":volumeId"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "volumeId",
                  "value": "{{volumeId}}"
                }
              ]
            },
            "description": "perm: `volume.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /projects/{projectId}/volumes/{volumeId}/resize",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/volumes/:volumeId/resize",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "volumes",
                ":volumeId",
                "resize"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "volumeId",
                  "value": "{{volumeId}}"
                }
              ]
            },
            "description": "perm: `volume.resize` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /projects/{projectId}/volumes/{volumeId}/usage",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/projects/:projectId/volumes/:volumeId/usage",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "projects",
                ":projectId",
                "volumes",
                ":volumeId",
                "usage"
              ],
              "variable": [
                {
                  "key": "projectId",
                  "value": "{{projectId}}"
                },
                {
                  "key": "volumeId",
                  "value": "{{volumeId}}"
                }
              ]
            },
            "description": "perm: `volume.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 replicas",
      "item": [
        {
          "name": "GET /replicas",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/replicas",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "replicas"
              ],
              "variable": []
            },
            "description": "perm: `replica.list` | auth: bearer"
          }
        },
        {
          "name": "GET /replicas/{replicaId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/replicas/:replicaId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "replicas",
                ":replicaId"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `replica.list` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 roles",
      "item": [
        {
          "name": "GET /roles",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/roles",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "roles"
              ],
              "variable": []
            },
            "description": "perm: `org.audit` | auth: bearer"
          }
        },
        {
          "name": "POST /roles",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/roles",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "roles"
              ],
              "variable": []
            },
            "description": "perm: `org.member.role` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /roles/{roleId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/roles/:roleId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "roles",
                ":roleId"
              ],
              "variable": [
                {
                  "key": "roleId",
                  "value": "{{roleId}}"
                }
              ]
            },
            "description": "perm: `org.audit` | auth: bearer"
          }
        },
        {
          "name": "PATCH /roles/{roleId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/roles/:roleId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "roles",
                ":roleId"
              ],
              "variable": [
                {
                  "key": "roleId",
                  "value": "{{roleId}}"
                }
              ]
            },
            "description": "perm: `org.member.role` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /roles/{roleId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/roles/:roleId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "roles",
                ":roleId"
              ],
              "variable": [
                {
                  "key": "roleId",
                  "value": "{{roleId}}"
                }
              ]
            },
            "description": "perm: `org.member.role` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /roles/{roleId}/permissions",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/roles/:roleId/permissions",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "roles",
                ":roleId",
                "permissions"
              ],
              "variable": [
                {
                  "key": "roleId",
                  "value": "{{roleId}}"
                }
              ]
            },
            "description": "perm: `org.audit` | auth: bearer"
          }
        },
        {
          "name": "PUT /roles/{roleId}/permissions",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PUT",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/roles/:roleId/permissions",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "roles",
                ":roleId",
                "permissions"
              ],
              "variable": [
                {
                  "key": "roleId",
                  "value": "{{roleId}}"
                }
              ]
            },
            "description": "perm: `org.member.role` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /roles/{roleId}/permissions/{permissionId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/roles/:roleId/permissions/:permissionId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "roles",
                ":roleId",
                "permissions",
                ":permissionId"
              ],
              "variable": [
                {
                  "key": "roleId",
                  "value": "{{roleId}}"
                },
                {
                  "key": "permissionId",
                  "value": "{{permissionId}}"
                }
              ]
            },
            "description": "perm: `org.member.role` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /roles/{roleId}/permissions/{permissionId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/roles/:roleId/permissions/:permissionId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "roles",
                ":roleId",
                "permissions",
                ":permissionId"
              ],
              "variable": [
                {
                  "key": "roleId",
                  "value": "{{roleId}}"
                },
                {
                  "key": "permissionId",
                  "value": "{{permissionId}}"
                }
              ]
            },
            "description": "perm: `org.member.role` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 servers",
      "item": [
        {
          "name": "GET /servers",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/servers",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "servers"
              ],
              "variable": []
            },
            "description": "perm: `server.register` | auth: bearer"
          }
        },
        {
          "name": "POST /servers",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/servers",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "servers"
              ],
              "variable": []
            },
            "description": "perm: `server.register` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /servers/{id}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/servers/:id",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "servers",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{id}}"
                }
              ]
            },
            "description": "perm: `server.register` | auth: bearer"
          }
        },
        {
          "name": "POST /servers/{id}/heartbeat",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/servers/:id/heartbeat",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "servers",
                ":id",
                "heartbeat"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{id}}"
                }
              ]
            },
            "description": "perm: `server.register` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /servers/{id}/ssh",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/servers/:id/ssh",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "servers",
                ":id",
                "ssh"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{id}}"
                }
              ]
            },
            "description": "perm: `server.register` | auth: bearer"
          }
        },
        {
          "name": "[destructive] DELETE /servers/{id}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/servers/:id",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "servers",
                ":id"
              ],
              "variable": [
                {
                  "key": "id",
                  "value": "{{id}}"
                }
              ]
            },
            "description": "perm: `server.remove` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 traffic",
      "item": [
        {
          "name": "GET /traffic",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/traffic",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "traffic"
              ],
              "variable": []
            },
            "description": "perm: `traffic.read` | auth: bearer"
          }
        },
        {
          "name": "[destructive] DELETE /traffic",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/traffic",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "traffic"
              ],
              "variable": []
            },
            "description": "perm: `cache.purge` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /traffic/search",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/traffic/search",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "traffic",
                "search"
              ],
              "variable": []
            },
            "description": "perm: `traffic.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 usage",
      "item": [
        {
          "name": "GET /usage",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/usage",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "usage"
              ],
              "variable": []
            },
            "description": "perm: `analytics.read` | auth: bearer"
          }
        },
        {
          "name": "GET /usage/bandwidth",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/usage/bandwidth",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "usage",
                "bandwidth"
              ],
              "variable": []
            },
            "description": "perm: `analytics.read` | auth: bearer"
          }
        },
        {
          "name": "GET /usage/requests",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/usage/requests",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "usage",
                "requests"
              ],
              "variable": []
            },
            "description": "perm: `analytics.read` | auth: bearer"
          }
        },
        {
          "name": "GET /usage/timeseries",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/usage/timeseries",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "usage",
                "timeseries"
              ],
              "variable": []
            },
            "description": "perm: `analytics.read` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 users",
      "item": [
        {
          "name": "GET /users/me",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/users/me",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "users",
                "me"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: bearer"
          }
        },
        {
          "name": "PATCH /users/me",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "PATCH",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/users/me",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "users",
                "me"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /users/me",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/users/me",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "users",
                "me"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /users/me/api-keys",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/users/me/api-keys",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "users",
                "me",
                "api-keys"
              ],
              "variable": []
            },
            "description": "perm: `apikey.create` | auth: bearer"
          }
        },
        {
          "name": "POST /users/me/api-keys",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/users/me/api-keys",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "users",
                "me",
                "api-keys"
              ],
              "variable": []
            },
            "description": "perm: `apikey.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /users/me/api-keys/{keyId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/users/me/api-keys/:keyId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "users",
                "me",
                "api-keys",
                ":keyId"
              ],
              "variable": [
                {
                  "key": "keyId",
                  "value": "{{keyId}}"
                }
              ]
            },
            "description": "perm: `apikey.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /users",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/users",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "users"
              ],
              "variable": []
            },
            "description": "perm: `user.list` | auth: bearer"
          }
        },
        {
          "name": "POST /users",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/users",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "users"
              ],
              "variable": []
            },
            "description": "perm: `user.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /users/{username}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/users/:username",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "users",
                ":username"
              ],
              "variable": [
                {
                  "key": "username",
                  "value": "{{username}}"
                }
              ]
            },
            "description": "perm: `user.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        }
      ]
    },
    {
      "name": "10 version",
      "item": [
        {
          "name": "GET /version",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));",
                  "pm.test('public route reachable', () => pm.expect(pm.response.code).to.not.equal(401));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [],
            "url": {
              "raw": "{{base_url}}/version",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "version"
              ],
              "variable": []
            },
            "description": "perm: `none` | auth: public"
          }
        }
      ]
    },
    {
      "name": "10 vms",
      "item": [
        {
          "name": "GET /vms",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms"
              ],
              "variable": []
            },
            "description": "perm: `replica.list` | auth: bearer"
          }
        },
        {
          "name": "GET /vms/{replicaId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `replica.list` | auth: bearer"
          }
        },
        {
          "name": "POST /vms/{replicaId}/start",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/start",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "start"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `replica.start` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /vms/{replicaId}/stop",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/stop",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "stop"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `replica.stop` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /vms/{replicaId}/restart",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/restart",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "restart"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `replica.restart` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /vms/{replicaId}/snapshot",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/snapshot",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "snapshot"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `replica.snapshot` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] POST /vms/{replicaId}/restore",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/restore",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "restore"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `replica.restore` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] POST /vms/{replicaId}/recover",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/recover",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "recover"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `replica.restore` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] DELETE /vms/{replicaId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `replica.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /vms/{replicaId}/domains",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/domains",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "domains"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `domain.list` | auth: bearer"
          }
        },
        {
          "name": "GET /vms/{replicaId}/logs",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/logs",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "logs"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `log.read` | auth: bearer"
          }
        },
        {
          "name": "GET /vms/{replicaId}/logs/stream",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/logs/stream",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "logs",
                "stream"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `log.read` | auth: bearer"
          }
        },
        {
          "name": "GET /vms/{replicaId}/metrics",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/metrics",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "metrics"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `metric.read` | auth: bearer"
          }
        },
        {
          "name": "GET /vms/{replicaId}/traffic",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/traffic",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "traffic"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `traffic.read` | auth: bearer"
          }
        },
        {
          "name": "GET /vms/{replicaId}/health",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/health",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "health"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `replica.list` | auth: bearer"
          }
        },
        {
          "name": "GET /vms/{replicaId}/ssh-info",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/ssh-info",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "ssh-info"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `ssh.connect` | auth: bearer"
          }
        },
        {
          "name": "[destructive] POST /vms/{replicaId}/ssh-cert",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/ssh-cert",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "ssh-cert"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `ssh.connect` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "[destructive] POST /vms/{replicaId}/exec",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/exec",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "exec"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `replica.exec` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /vms/{replicaId}/console",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/vms/:replicaId/console",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "vms",
                ":replicaId",
                "console"
              ],
              "variable": [
                {
                  "key": "replicaId",
                  "value": "{{replicaId}}"
                }
              ]
            },
            "description": "perm: `console.open` | auth: bearer"
          }
        }
      ]
    },
    {
      "name": "10 volumes",
      "item": [
        {
          "name": "GET /volumes",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/volumes",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "volumes"
              ],
              "variable": []
            },
            "description": "perm: `volume.read` | auth: bearer"
          }
        },
        {
          "name": "POST /volumes",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/volumes",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "volumes"
              ],
              "variable": []
            },
            "description": "perm: `volume.create` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /volumes/{volumeId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/volumes/:volumeId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "volumes",
                ":volumeId"
              ],
              "variable": [
                {
                  "key": "volumeId",
                  "value": "{{volumeId}}"
                }
              ]
            },
            "description": "perm: `volume.read` | auth: bearer"
          }
        },
        {
          "name": "[destructive] DELETE /volumes/{volumeId}",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "DELETE",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/volumes/:volumeId",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "volumes",
                ":volumeId"
              ],
              "variable": [
                {
                  "key": "volumeId",
                  "value": "{{volumeId}}"
                }
              ]
            },
            "description": "perm: `volume.delete` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "POST /volumes/{volumeId}/resize",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "POST",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              },
              {
                "key": "X-CSRF-Token",
                "value": "{{csrf}}"
              },
              {
                "key": "Content-Type",
                "value": "application/json"
              }
            ],
            "url": {
              "raw": "{{base_url}}/volumes/:volumeId/resize",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "volumes",
                ":volumeId",
                "resize"
              ],
              "variable": [
                {
                  "key": "volumeId",
                  "value": "{{volumeId}}"
                }
              ]
            },
            "description": "perm: `volume.resize` | auth: bearer",
            "body": {
              "mode": "raw",
              "raw": "{}"
            }
          }
        },
        {
          "name": "GET /volumes/{volumeId}/usage",
          "event": [
            {
              "listen": "test",
              "script": {
                "type": "text/javascript",
                "exec": [
                  "// Every route must answer; a 5xx is always a bug. 401/403/404 are valid",
                  "// outcomes when the collection variable for a resource is unset.",
                  "pm.test('no 5xx', () => pm.expect(pm.response.code).to.be.below(500));"
                ]
              }
            }
          ],
          "request": {
            "method": "GET",
            "header": [
              {
                "key": "Authorization",
                "value": "Bearer {{token}}"
              }
            ],
            "url": {
              "raw": "{{base_url}}/volumes/:volumeId/usage",
              "host": [
                "{{base_url}}"
              ],
              "path": [
                "volumes",
                ":volumeId",
                "usage"
              ],
              "variable": [
                {
                  "key": "volumeId",
                  "value": "{{volumeId}}"
                }
              ]
            },
            "description": "perm: `volume.read` | auth: bearer"
          }
        }
      ]
    }
  ]
}
